# RunPod 기본
runpod==1.6.0

# PyTorch 및 오디오 처리
torch>=2.0.0
torchaudio>=2.0.0

# Transformers 및 모델 관련
transformers>=4.35.0
accelerate>=0.25.0
safetensors>=0.4.0
huggingface-hub>=0.19.0

# Zonos TTS 의존성
# zonos 패키지가 없으므로 필요한 의존성들을 직접 설치
# descript-dac==1.0.0  # DAC autoencoder - PyPI에 없음, 별도 설치 필요
phonemizer>=3.2.1
inflect>=7.0.0
kanjize>=1.3.2
sudachipy>=0.6.7
sudachidict-core>=20230927

# 기타 유틸리티
numpy>=1.24.0
sentencepiece>=0.1.99
boto3>=1.28.0  # S3 업로드용 (선택사항)
aiofiles>=23.0.0  # 비동기 파일 처리용

# 오디오 처리 추가 라이브러리
librosa>=0.10.0  # 오디오 분석용 (선택사항)
soundfile>=0.12.0  # 다양한 오디오 포맷 지원

# espeak-ng 의존성 (phonemizer를 위해)
# 시스템 레벨에서 설치 필요: apt-get install espeak-ng